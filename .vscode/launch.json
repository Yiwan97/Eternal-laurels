{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/eternal-laurels/package.json",
                "localRoot": "${workspaceFolder}/eternal-laurels"
            }
        }
    ]
}